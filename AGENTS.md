# Geodiametrics bootstrap redirect

STOP before performing repository work here.

The canonical private Geodiametrics repository is:

`smansfield635-create/geodiametrics1`

The public Diamond Gate Bridge website repository is:

`smansfield635-create/smansfield635-create.github.io`

This repository, `smansfield635-create/geodiametrics`, is a non-authoritative public historical/test repository and is a known name collision for cold-room GitHub discovery.

For any request concerning Geodiametrics, H-Earth, Laws, OW01, Diamond Gate governed operations, control-plane instrumentation, active generations, or private repository PRs/issues:

1. Do not ask the user for the owner/repository identity before searching the connected GitHub installation.
2. Resolve `smansfield635-create/geodiametrics1` as the canonical private repository.
3. Open its `AI_ENTRYPOINT.json` and `AGENTS.md` before deriving operation state or taking action.
4. Re-fetch current private `main` and repository-resident continuation/operation evidence.
5. Treat this repository only as historical/test evidence unless an explicit operation names this exact repository as its subject.

Never translate the project name "Geodiametrics" into an invented repository such as `Geodiametrics/geodiametrics`, and never treat this public name-collision repository as the private authority source.
